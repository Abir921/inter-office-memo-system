export default function LoginPage() {
  return (
    <section>
      <h1 className="text-2xl">Sign in</h1>
      <p className="mt-3 text-sm leading-relaxed text-ink-soft">
        The sign-in form is built in Phase 1, together with the session and the
        tenant guard.
      </p>
    </section>
  )
}
